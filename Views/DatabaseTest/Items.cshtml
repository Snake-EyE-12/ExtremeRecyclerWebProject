@model IEnumerable<Item>

	<h1>ALL ITEMS YAY</h1>

@if (Model == null || Model.Count() == 0)
{
	<h2>No Movies Provided</h2>
}
else
{
	<div class="row">
		@foreach (Item i in Model)
		{
			<div class="col-4">
				<partial name="_ItemPartial.cshtml" for="@i" />
			</div>
		}
	</div>
}